<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.forrestfan.beam">

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:largeHeap="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DepressionGroup"
            android:label="Depression Support Group" />
        <activity
            android:name=".Search"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".SearchResults"
            android:label="Search Results" />
        <activity
            android:name=".AllGroups"
            android:label="Search Results" />
        <activity
            android:name=".ChildAbuse"
            android:label="Child Abuse" />
        <activity
            android:name=".RequestGroup"
            android:label="Request A Group" />
        <activity
            android:name=".RequestSubmitted"
            android:theme="@style/NoActionBar" />
        <activity
            android:name=".Talk"
            android:label="@string/title_activity_talk"></activity>
    </application>

</manifest>